# === Soulseek Account ===
# Your Soulseek username and password (create at https://www.slsknet.org/)
SLSKD_SLSK_USERNAME=your_soulseek_username
SLSKD_SLSK_PASSWORD=your_soulseek_password

# === Spotify API ===
# Create an app at https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret

# === slskd Settings ===
# Shared secret between the backend and slskd containers — NOT an external
# credential. You don't fetch this from anywhere; it's just any random string
# of 16+ characters (slskd requires minimum 16).
#
# Leave the placeholder below as-is: start.sh will auto-generate a random
# value the first time you run it. To regenerate later, just delete the
# line and re-run start.sh, or replace manually with e.g.:
#   openssl rand -hex 16
SLSKD_API_KEY=supersecretapikey123change_me

# === Optional: Music directory to share on Soulseek ===
# Path to your local music library (shared with other Soulseek users)
# MUSIC_DIR=/path/to/your/music
